# Fabric item definitions are uploaded byte-for-byte (base64) by Deploy-PBICatalog.ps1.
# Normalise to LF in the repo AND on checkout so cloned artifacts match the export exactly.
* text=auto eol=lf

# PowerShell is Windows-first; CRLF is the native convention.
*.ps1 text eol=crlf
